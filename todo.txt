
things to consider adding:
- NBNS (NetBIOS Name Service)
- Passive ARP monitoring

Prioritization order:
ARP → mDNS → SSDP → NBNS → passive monitoring → port probes.


Visualization:

    cli client:
        store hosts in a JSON format using cJSON

    python script:
        Read JSON
        Create topology using pyvis and display on browser

    FOrget this but keep in mind:    
        Topology Class
        Host Information Class
        A way to save data from the maintool in c to the topology tool in python (most likely xml cause nmap uses that?)

