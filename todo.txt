
things to consider adding:
- NBNS (NetBIOS Name Service)
- Passive ARP monitoring
- TCP port probing
- DNS PTR lookups

Prioritization order:
ARP → mDNS → SSDP → NBNS → passive monitoring → port probes.

Fingerprinting:
MAC OUI + DHCP hostname + mDNS device

Things to implement:
    Refactor ht_destroy function into void ht_destroy(struct HashTable* ht, void (*destroy_value)(void*));
        - this allows seperate destruction for each type like device_entry and mdns_service structs
        - seperate destructor functions for each type
    
    Keep track of service_entries in a hash table
        - 


    Debugging macros for ease of Debugging


https://en.ittrip.xyz/c-language/c-debugging-macro

https://sqlpey.com/c/c-cpp-debugging-macros/